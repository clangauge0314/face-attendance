{
  "name": "basic-electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "cd ../frontend && npm run build && cd ../electron && electron ."
  },
  "dependencies": {
    "electron": "^39.2.3"
  }
}
